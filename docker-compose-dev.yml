services:
  frontend:
    build: 
      context: ./frontend
      target: dev
    volumes:
      - ./frontend:/app 
  backend:
      build:
        context: ./backend
        target: dev
      volumes:
      - ./backend:/app 
  solver:
      build:
        context: ./solver
        target: dev
      volumes:
      - ./solver:/app 
